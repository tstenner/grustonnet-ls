# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/test.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
env:
  CARGO_TERM_COLOR: always
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os-name: Linux-x86_64
            runs-on: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
          - os-name: Windows-x86_64
            runs-on: windows-latest
            target: x86_64-pc-windows-gnu
          #- os-name: macOS-arm64
          #  runs-on: macos-latest
          #  target: aarch64-apple-darwin

    runs-on: ${{ matrix.platform.runs-on }}
    steps:
      - uses: actions/checkout@v6
      - name: env
        run: go version && cargo version && go env
      - name: Run tests
        env:
          RUST_BACKTRACE: 1
          CARGO_PROFILE_DEV_BUILD_OVERRIDE_DEBUG: true
        run: cd crates/jsonnet-bridge && cargo build --target ${{ matrix.platform.target }} && cargo test --verbose --target ${{ matrix.platform.target }}
'on':
  pull_request: {}
  push: {}
